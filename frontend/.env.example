# คัดลอกไฟล์นี้เป็น .env.local แล้วใส่ URL ของ backend จริง
# ตอนพัฒนาบนเครื่อง (localhost) ไม่ต้องสร้างไฟล์นี้ก็ได้ App จะ fallback ไปที่ http://127.0.0.1:8000 เอง
VITE_API_URL=https://your-backend-url.hf.space
